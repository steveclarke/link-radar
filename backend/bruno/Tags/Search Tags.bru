meta {
  name: Search Tags
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tags?search=ruby
  body: none
  auth: inherit
}

params:query {
  search: ruby
}

docs {
  Search tags by name for autocomplete functionality.

  Query parameters:
  - search: Search term (case-insensitive partial match)

  Returns:
  {
    "data": {
      "tags": [array of up to 20 matching tags ordered by usage count]
    }
  }

  This endpoint is designed for LLM integration and autocomplete UI.

  Example searches:
  - "java" → finds "JavaScript", "Java"
  - "rail" → finds "Rails", "Railway"
}


