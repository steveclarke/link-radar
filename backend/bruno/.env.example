# Bruno Environment Variables
# Copy this file to .env and customize for your local environment
# The .env file is gitignored to allow machine-specific configuration

# Rails server port (should match RAILS_PORT in backend/.env)
# Default: 3000 (Rails default)
RAILS_PORT=3000

# API Key for development
API_KEY=dev_api_key_change_in_production
