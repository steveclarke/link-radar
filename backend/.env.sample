# =============================================================================
# LinkRadar Backend Environment Configuration
# =============================================================================
# This file contains sample environment variables for local development.
# Copy this file to `.env` and uncomment/modify values as needed.

# =============================================================================
# RAILS CREDENTIALS & 1PASSWORD
# =============================================================================
# The setup script can automatically fetch master.key from 1Password CLI.
# If you don't have 1Password CLI, see alternatives below.

# Skip 1Password CLI fetch (set to true if you don't use 1Password)
# SKIP_ONEPASSWORD=true

# Customize which 1Password item to fetch master.key from (optional)
# MASTER_KEY_OP_ITEM_ID=your-item-id
# MASTER_KEY_OP_VAULT=LinkRadar
# MASTER_KEY_OP_FIELD=credential

# =============================================================================
# RAILS SERVER
# =============================================================================
# Rails development server port (default: 3000)
# PORT=3000

# =============================================================================
# POSTGRESQL DATABASE
# =============================================================================
# PostgreSQL username (default: postgres)
# POSTGRES_USER=postgres

# PostgreSQL password (default: postgres)
# POSTGRES_PASSWORD=postgres

# PostgreSQL host (default: localhost)
# POSTGRES_HOST=localhost

# PostgreSQL port - used by Docker Compose AND Rails connection (default: 5432)
# POSTGRES_PORT=5432

# =============================================================================
# DOCKER COMPOSE SERVICES
# =============================================================================
# Additional service port mappings for Docker Compose.
# Only uncomment if you need to override defaults.

# Redis port (default: 6379)
# REDIS_PORT=6379

# MailDev web interface port (default: 1080)
# MAILDEV_WEB_PORT=1080

# MailDev SMTP server port (default: 1025)
# MAILDEV_SMTP_PORT=1025

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Optional overrides for application settings.
# Defaults are defined in config/configs/*.rb

# Comma-separated list of origins for CORS
# CORE_CORS_ORIGINS=http://localhost:9000

# Log level (debug, info, warn, error, fatal)
# CORE_LOG_LEVEL=debug

# Frontend URL for generating links in emails, etc.
# CORE_FRONTEND_URL=http://localhost:9000
