# =============================================================================
# LinkRadar Backend Environment Configuration
# =============================================================================
# This file contains sample environment variables for local development.
# Copy this file to `.env` and uncomment/modify values as needed.

# =============================================================================
# RAILS SERVER CONFIGURATION
# =============================================================================
# Rails development server port (default: 3000)
# Change if another app is using port 3000
# PORT=3000

# =============================================================================
# POSTGRESQL DATABASE CONFIGURATION
# =============================================================================
# These variables configure BOTH Docker Compose (PostgreSQL setup) and Rails
# (database connection). The same credentials/settings are used for both.
#
# Uncomment and modify if you need non-default values:

# PostgreSQL username (default: postgres)
# POSTGRES_USER=postgres

# PostgreSQL password (default: postgres)
# POSTGRES_PASSWORD=postgres

# Database host for Rails connection (default: localhost)
# POSTGRES_HOST=localhost

# PostgreSQL port - used by BOTH Docker mapping AND Rails connection
# Change this if port 5432 conflicts on your machine
# POSTGRES_PORT=5432

# =============================================================================
# DOCKER COMPOSE SERVICE OVERRIDES - BEGIN
# =============================================================================
# Additional service port mappings for Docker Compose.
# ONLY uncomment if you need to override the defaults.

# Redis port (change if 6379 is already in use)
# REDIS_PORT=6379

# Maildev web interface port
# MAILDEV_WEB_PORT=1080

# Maildev SMTP server port
# MAILDEV_SMTP_PORT=1025

# =============================================================================
# DOCKER COMPOSE SERVICE OVERRIDES - END
# =============================================================================

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# NOTE: These variables are only needed if you need to override them;
# otherwise they are set to sensible defaults in config/configs/*.rb

### CORE

# Comma-separated list of origins for CORS
# CORE_CORS_ORIGINS=http://localhost:9000

# Log level (debug, info, warn, error, fatal)
# CORE_LOG_LEVEL=debug

# Frontend URL for generating links in emails, etc.
# CORE_FRONTEND_URL=http://localhost:9000
