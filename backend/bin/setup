#!/usr/bin/env ruby
require_relative "../lib/link_radar/support"

# Run the setup runner
APP_ROOT = File.expand_path("..", __dir__)
LinkRadar::Support::SetupRunner.new(APP_ROOT).run(ARGV) if __FILE__ == $0
