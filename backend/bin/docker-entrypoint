#!/bin/bash -e

# If running the rails server (directly or via Thruster), prepare the database
if [ "${1}" == "./bin/thrust" ] || \
   { [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; }; then
  ./bin/rails db:prepare
fi

exec "${@}"
